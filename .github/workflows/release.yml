name: Release materials

on:
  push:
    tags:
      - "L*-v*"

jobs:
  release:
    permissions:
      contents: write
      pages: write
      id-token: write
    uses: CUNI-NATUR-Biostatistics/.github/.github/workflows/release.yml@main
    secrets: inherit
